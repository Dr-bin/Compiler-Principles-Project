// PL/0 子集示例：完整运算符测试
// 测试所有算术运算符：+, -, *, /

var a, b, c, d, e;

begin
    a = 10;
    b = 3;
    
    // 测试减法
    c = a - b;
    write(c);
    
    // 测试除法
    d = a / b;
    write(d);
    
    // 测试复合运算
    e = a + b - c * d / 2;
    write(e);
end
