// PL/0 子集示例：if / while / 关系运算
// 注意：LL(1)文法不支持else（悬空else冲突）

var x, y, max, i;

x = 3;
y = 5;

// if语句：设置初始值，如果条件满足则覆盖
max = x;
if (x < y) 
    max = y;

write(max);

// while: 从 0 累加到 4
i = 0;
while (i < 5)
{
    write(i);
    i = i + 1;
}










